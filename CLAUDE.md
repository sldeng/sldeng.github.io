# Claude Code Skills

## check-travels

检查项目中的旅行文件夹是否有新增，并自动更新travel-data.json配置。

**触发时机：**
- 添加了新的旅行文件夹（以"行"结尾）
- 在旅行文件夹下添加了新的地点子文件夹
- 修改了文件夹结构后

**执行操作：**
- 扫描项目根目录下所有以"行"结尾的文件夹
- 检查每个文件夹下的子文件夹（地点）
- 自动更新travel-data.json配置文件

**示例：**
```
/check-travels
```
